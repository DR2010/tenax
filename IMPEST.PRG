use estrotin
index on codrot to rot

estrotin->( dbgotop() )
set print on
set device to print
set printer to "rotina.txt"

while estrotin->( ! eof() )
   ? codrot + '  ' + descrot

   estrotin->( dbskip() )

end

set printer to
set print off
set device to screen


